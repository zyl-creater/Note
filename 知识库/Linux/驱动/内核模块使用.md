
## 什么是内核模块

大部分Unix内核是单块结构（monolithic kernel）：每一个内核层都被集成到整个内核程序中，并代表当前进程在内核态下运行。相反，微内核（micro kernel）操作系统只需要内核有一个很小的函数集，通常包括几个*同步原语*、一个简单的*调度程序*和*进程间通讯机制*。这两种体系的内核各有优缺点。使用微内核的操作系统具有很好的可扩展性而且内核非常的小，但这样的操作系统由于不同层次之间显式的消息传递要花费一定的代价所以效率比较低。对单一体系结构的操作系统来说，所有的模块都集成在一起，系统的速度和性能都很好，但是可扩展性和维护性就相对比较差。
为了达到微内核理论上的很多优点而又不影响性能，Linux内核提供了模块（module）。模块是一个目标文件，其代码可以在运行时链接到内核或从内核解除链接。这种目标代码通常由一组函数组成，用来实现文件系统、驱动程序或其他内核上层功能。与微内核操作系统的外层不同，模块不是作为一个特殊的进程执行的。相反，与任何其他静态链接的内核函数一样，它代表当前进程在内核态下执行。

使用模块的主要优点：
    1. 模块化方法
        因为任何模块都可以在运行时被链接或解除链接,因此,系统程序员必须提出良定义的软件接口以访问由模块处理的数据结构。这使得开发新模块变得容易。
    2. 平台无关性
        即使模块依赖于某些特殊的硬件特点，但它不依赖于某个固定的硬件平台。例如，符合SCSI标准的磁盘驱动程序模块，在IBM兼容PC与HP的 Alpha机上都能很好地工作。
    3. 节省内存使用
        当需要模块功能时，把它链接到正在运行的内核中，否则，将该糗块解除链接。这种机制对于小型嵌入式系统是非常有用的。
    4. 无性能损失
        模块的目标代码一旦被链接到内核，其作用与静态链接的内核的目标代码完全等价。因此，当模块的函数被调用时，无需显式地进行消息传递（当模块被懿接或被解除链接时，性能稍有下降。但是在微内核操作系统中、系统进程的创建和删除也是这样的)。

## 编写简单内核模块

下面是一个完整的 hello world 模块

`helloworld.c`
```c
#include <linux/init.h>
#include <linux/module.h>

static void hello(void);

static void hello()
{
    printk(KERN_INFO "Hello china \n");
}

static int __init hello_init(void)
{
    printk(KERN_INFO "init modules\n");
    hello();
    return 0;
}

static void __exit hello_exit(void)
{
    printk(KERN_INFO "exit modules\n");
}

module_init(hello_init);
module_exit(hello_exit);
MODULE_LICENSE("GPL");
```

说明：
    1.  任何模块程序的编写都需要包含 linux/module.h 这个头文件，这个文件包含了对模块的结构定义以及模块的版本控制。文件里的主要数据结构我们会在后面详细介绍。
    2.  函数 hello_init() 和函数 hello_exit() 是模块编程中最基本的也是必须的两个函数。hello_init() 向内核注册模块所提供的新功能；hello_exit() 负责注销所有由模块注册的功能。
    3.  注意我们在这儿使用的是 printk() 函数（不要习惯性地写成printf），printk() 函数是由 Linux内核定义的，功能与 printf 相似。字符串 KERN_INFO 表示消息的优先级，printk() 的一个特点就是它对于不同优先级的消息进行不同的处理。
    4. 我们可以使用 insmod 和 rmmod 来测试加载和移除模块，注意只有超级用户才能加载和卸载模块。

## 文件编译流程
一般来说，无论是 C、C++，首先要把源文件编译成中间代码文件，在 Windows 下也就是 .obj 文件，UNIX 下是 .o 文件，即 Object File，这个动作叫做编译（compile）。然后再把大量的 Object File 合成执行文件，这个动作叫作链接（link）。 编译时，编译器需要的是语法的正确，函数与变量的声明的正确。对于后者，通常是你需要告诉编译器头文件的所在位置（头文件中应该只是声明，而定义应该放在 C/C++文件中），只要所有的语法正确，编译器就可以编译出中间目标文件。一般来说，每个源文件都应该对应于一个中间目标文件（O 文件或是 OBJ 文件）。链接时，主要是链接函数和全局变量，所以，我们可以使用这些中间目标文件（O 文件或是 OBJ文件）来链接我们的应用程序。链接器并不管函数所在的源文件，只管函数的中间目标文件（Object File），在大多数时候，由于源文件太多，编译生成的中间目标文件太多，而在链接时需要明显地指出中间目标文件名，这对于编译很不方便，所以，我们要给中间目标文件打个包，在 Windows 下这种包叫“库文件”（Library File)，也就是 .lib 文件，在 UNIX下，是 Archive File，也就是 .a 文件。
源文件首先会生成中间目标文件，再由中间目标文件生成执行文件。在编译时，编译器只检测程序语法，和函数、变量是否被声明。如果函数未被声明，编译器会给出一个警告，但可以生成 Object File。而在链接程序时，链接器会在所有的 Object File 中找寻函数的实现，如果找不到，那到就会报链接错误码（Linker Error）。

## 文件的推入
我们编译生成的 ko 文件还在我们的电脑上，并没有在我们的板子中，这时，我们可以使用 ADB 工具来将 ko 文件推入板子中

在Android O之后的版本中，我们知道很多ko被从kernel中提取出来，移动到system分区和vendor分区中保存。那么系统启动在服务加载之前首先需要把内核驱动模块全部加载起来才可以，所以我们的system分区和vendor分区必须要先挂载起来。

确保已经拥有 ADB 环境后，先使用下面命令来确定设备是否已经识别到
```cmd
adb devices
```
运行结果如下
```cmd
C:\Users\Administrator>adb devices
List of devices attached
UG202109015434  device
```

在确定设备已经识别到之后
可以使用下面命令进行挂载
```cmd
adb root
adb remount
```

**注意**：
在第一次挂载分区的时候，需要解锁设备
    1. 进入设置-->关于，多次点击版本号，即可打开开发者模式；
    2. 进入系统-->开发者选项，打开“使用开发者选项”，打开“USB”调试选项，打开“OEM解锁”，点击启动；
在执行下面命令
```cmd
adb reboot bootloader
fastboot flashing unlock
fastboot reboot
adb root
adb disable-verity
adb reboot
adb root
adb remount
```

_adb_ _disable-verity_ 关闭分区检测功能

在每次重启后，在将文件 push 到 `/system` 或者 `/vendor` 文件夹之前，必须先输入命令 `adb remount`。
`adb remount` 的作用相当于 `adb shell mount -o rw,remount,rw /system` 。

确定分区挂载成功后，使用 `adb push` 命令将 ko 文件推入板子的对应目录中
```cmd
C:\Users\Administrator>adb push X:\amlogic\q\q-amlogic-20210208-gtvs.xml\vendor\sdmc\drivers\test\helloworld.ko /vendor/lib/modules/
5295 KB/s (144116 bytes in 0.026s)
```

## 模块的加载和卸载

ko文件成功推入板子后，就要开始模块的加载

我们可以使用 `lsmod` 命令来查看已经加载的模块
```shell
console:/ # lsmod
Module                  Size  Used by
dhd                  2027520  0 
dts_sys_led            16384  0 
btusb                  90112  0 
media_sync             20480  0 
dvb_demux             192512  1 media_sync
aml_aucpu              40960  1 dvb_demux
galcore               385024  0 
dovi                  929792  0 
mali_kbase            487424  13 
amvdec_av1            155648  0 
amvdec_mavs            57344  0 
amlogic_fbc_lib        32768  0 
```

ko 文件在板子下的目录一般都在 `/vendor/lib/modules` 目录下
```shell
console:/vendor/lib/modules # ls 
8188eu.ko           amvdec_mmjpeg.ko  modules.alias               
8189es.ko           amvdec_mmpeg12.ko modules.dep               
8821cu.ko           amvdec_vp9.ko     ssv_hwif_ctrl.ko            
8822bs.ko           bcmdhd.ko         stream_input.ko             
amvdec_h264mvc.ko   galcore.ko        wlan_mt76x8_sdio.ko         
amvdec_h265.ko      helloworld.ko     wlan_mt76x8_usb.ko          
amvdec_mavs.ko      mali.ko           wlan_resident_206x.ko       
amvdec_mh264.ko     media_clock.ko
```

我们可以使用 `insmod` 命令来加载模块
```shell
console:/vendor/lib/modules # insmod helloworld.ko 
console:/vendor/lib/modules # dmesg
[  755.696538] init modules
[  755.696546] Hello china
```

用 lsmod 来看模块是否加载
```shell
console:/vendor/lib/modules # lsmod
Module                  Size  Used by
helloworld             16384  0 
dhd                  2027520  0 
dts_sys_led            16384  0 
```

使用 `rmmod` 命令来卸载模块
```shell
console:/vendor/lib/modules # rmmod helloworld.ko                              
console:/vendor/lib/modules # dmesg
[ 1020.735173] exit modules
```

